# Semantic requirements for cmdstanpy. A locked requirements file can be
# generated by installing the `pip-tools` package from pypi and running
# `pip-compile`.
-e file:.[all,docs,tests]
# Dev requirements that are not part of the package.
pip-tools
